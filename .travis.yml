language: java

sudo: false

jdk:
  - oraclejdk7

cache:
  directories:
    - '$HOME/.m2/repository'

services:
  - postgresql

addons:
  postgresql: '9.4'

before_script:
    - psql -U postgres -c 'create database datability;'